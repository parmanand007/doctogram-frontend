version: '3.8'
services:
  frontend:
    build: .
    image: doctogram-frontend:latest
    container_name: doctogram-frontend
    ports:
      - '3000:80'
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://host.docker.internal:8000/api}
    restart: unless-stopped